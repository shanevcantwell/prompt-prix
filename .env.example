# ═══════════════════════════════════════════════════════════════════════════
# prompt-prix Configuration
# ═══════════════════════════════════════════════════════════════════════════
# Copy this file to .env and customize for your setup.
# Lines starting with # are comments. Uncomment to enable.

# ───────────────────────────────────────────────────────────────────────────
# LM Studio Servers
# ───────────────────────────────────────────────────────────────────────────
# Configure your LM Studio server endpoints. Add as many as needed.
# Work-stealing dispatcher will distribute tests across all available servers.

LM_STUDIO_SERVER_1=http://192.168.1.10:1234
LM_STUDIO_SERVER_2=http://192.168.1.11:1234
# LM_STUDIO_SERVER_3=http://192.168.1.12:1234

# Local server example:
# LM_STUDIO_SERVER_1=http://127.0.0.1:1234

# ───────────────────────────────────────────────────────────────────────────
# Gradio UI
# ───────────────────────────────────────────────────────────────────────────
# Port for the Gradio web interface.
# Change if 7860 conflicts with other apps (Forge, Fooocus, etc.)

GRADIO_PORT=7860

# ───────────────────────────────────────────────────────────────────────────
# Battery Mode - Retry Settings
# ───────────────────────────────────────────────────────────────────────────
# Retry configuration for transient errors (model loading, connection issues).
# Uses exponential backoff: wait doubles each retry up to max.
#
# Increase these for large models (27B+) that take longer to load:
#   BATTERY_RETRY_ATTEMPTS=5
#   BATTERY_RETRY_MIN_WAIT=10
#   BATTERY_RETRY_MAX_WAIT=60

# Number of retry attempts before giving up (default: 3)
# BATTERY_RETRY_ATTEMPTS=3

# Minimum wait between retries in seconds (default: 4)
# BATTERY_RETRY_MIN_WAIT=4

# Maximum wait between retries in seconds (default: 30)
# BATTERY_RETRY_MAX_WAIT=30

# ───────────────────────────────────────────────────────────────────────────
# Beyond Compare Integration (Optional)
# ───────────────────────────────────────────────────────────────────────────
# Path to Beyond Compare executable for side-by-side model output diff.
# Leave commented to use platform defaults:
#   Windows: C:\Program Files\Beyond Compare 4\BComp.exe
#   Linux/Mac: bcompare (must be in PATH)

# BEYOND_COMPARE_PATH=/usr/bin/bcompare
